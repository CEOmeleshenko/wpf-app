<Page x:Class="ExamApp.Pages.RequestListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ExamApp.Pages"
      mc:Ignorable="d"
      Title="Список заявок" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <WrapPanel Orientation="Horizontal">
            <Button Name="CreateRequestButton"
                    Content="Создать заявку"
                    Click="AddRequestButton_OnClick"
                    Width="120"
                    Margin="0 10" />
        </WrapPanel>

        <DataGrid Name="ItemsDataGrid"
                  AutoGenerateColumns="False"
                  Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер" Binding="{Binding Number}" />
                <DataGridTextColumn Header="Дата создания" Binding="{Binding Number}" />
                <DataGridTextColumn Header="Тип оборудования" Binding="{Binding Number}" />
                <DataGridTextColumn Header="Описание проблемы" Binding="{Binding Number}" />
                <DataGridTextColumn Header="Клиент" Binding="{Binding Number}" />
                <DataGridTextColumn Header="Статус" Binding="{Binding Number}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>